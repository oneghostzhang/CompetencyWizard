# CompetencyWizard 潛在問題報告

> 基於完整程式碼審查，涵蓋所有 .py 檔案。嚴重度分為 🔴 高 / 🟠 中 / 🟡 低。

---

## 一、執行時期 Crash 風險

### 🔴 高嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 1 | `IndexError`：搜尋結果為空時仍存取 `_std_radio_group[1]` | `wizard_ui.py` ~行 1069 | RAG 搜尋若無結果，radio group 只有 1 個元素，存取 index 1 會 crash |
| 2 | `AttributeError`：表格新增列時上一列的 item 為 None | `wizard_ui.py` ~行 1122 | `_table_add_row()` 呼叫 `prev_resp_code.text()`，若上一列該格為空，`.text()` 拋出例外 |
| 3 | `AttributeError`：LM Studio 回應的 content 為 None | `ai_chat.py` ~行 221 | `resp.choices[0].message.content` 可能為 None，呼叫 `.strip()` 直接 crash |
| 4 | `FAISS 向量維度不匹配` | `wizard_rag.py` ~行 263 | 若模型版本升級導致向量維度改變，舊快取載入後 search 會 crash |

### 🟠 中嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 5 | `JSONDecodeError`：LLM 生成不完整 JSON | `ai_chat.py` ~行 423 | `_worker_main()` 直接 `json.loads()`，LLM 輸出截斷時 process crash |
| 6 | `IndexError`：任務索引越界 | `wizard_ui.py` ~行 226 | `self.rows[idx]` 未加邊界檢查 |
| 7 | 職能等級非整數導致 `ValueError` | `wizard_ui.py` ~行 1155 | `int(cell(5))` 若使用者在表格輸入文字（如 "N/A"）會 crash |

---

## 二、多執行緒 / 多程序安全

### 🔴 高嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 8 | 競態條件：`_stop` 標誌無同步機制 | `wizard_ui.py` ~行 195 | 主執行緒設 `_stop=True` 時，worker process 可能正在讀取相同狀態 |
| 9 | Process 資源未正確清理 | `wizard_ui.py` ~行 237 | `proc.kill()` 在 process 已死亡時仍呼叫會拋例外；Queue 資源未釋放 |
| 10 | `InitThread` 無超時機制 | `wizard_ui.py` ~行 139 | 模型載入無限卡住時，UI 永遠停在載入頁，無法取消或重試 |
| 11 | 舊 LLM 執行緒未停止就重新執行 | `wizard_ui.py` ~行 1265 | `_run_llm()` 若使用者重複點擊，舊執行緒仍在背景跑，新舊結果交錯覆蓋 |

### 🟠 中嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 12 | `SearchThread` 無超時 | `wizard_ui.py` ~行 161 | RAG 搜尋卡住時 UI 完全凍結 |
| 13 | `ParseThread` 例外無法回報 UI | `wizard_ui.py` ~行 274 | PDF 解析失敗只寫 log，使用者看不到錯誤訊息 |
| 14 | Queue 資源洩漏 | `ai_chat.py` ~行 452 | process 異常終止時，`multiprocessing.Queue` 底層 pipe 未正確關閉 |

---

## 三、記憶體洩漏與資源未釋放

### 🔴 高嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 15 | FAISS Index 無卸載機制 | `wizard_rag.py` ~行 163 | 大型索引（300 MB+）載入後無 `del self._index`，記憶體常駐 |
| 16 | Embedding 模型無卸載機制 | `wizard_rag.py` ~行 90 | SentenceTransformer 模型（約 500 MB）無法手動卸載 |

### 🟠 中嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 17 | openpyxl Workbook 未顯式關閉 | `excel_exporter.py` ~行 346 | `wb = openpyxl.Workbook()` 後無 `wb.close()`，依賴 GC 釋放 |
| 18 | `deleteLater()` 時機不確定 | `wizard_ui.py` ~行 1005 | 清空 layout 時使用 `deleteLater()`，若 Qt 事件迴圈忙碌，widget 延遲釋放 |
| 19 | LlamaCpp 模型重複載入 | `ai_chat.py` ~行 408 | 子 process 多次啟動會重複載入模型，無快取機制 |

---

## 四、邊界條件未處理

### 🔴 高嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 20 | 零任務時後續流程仍執行 | `wizard_ui.py` ~行 1165 | `_goto_detail()` 雖有 MessageBox，但後續代碼仍假設 rows 非空 |
| 21 | `parsed_json_v2/` 目錄不存在 | `wizard_rag.py` ~行 25 | 預設 JSON 目錄不存在時，`_build_index()` 找到 0 個檔案並拋出 RuntimeError |
| 22 | TAIDE 模型路徑硬寫 Windows | `ai_chat.py` ~行 39 | 路徑 `C:\Users\User\...` 限定特定使用者，換機或換帳號立即失效 |

### 🟠 中嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 23 | RAG 搜尋結果為空後，流程繼續執行 | `wizard_ui.py` ~行 1022 | 使用者選「不使用基準」進入編輯器，若 rows 為空，後續 LLM 頁面會崩潰 |
| 24 | LLM behaviors 為空時流程卡住 | `wizard_ui.py` ~行 1283 | AI 生成空建議時，使用者看到紅色提示但無法操作，流程卡住 |
| 25 | Task Code 重複 | `wizard_ui.py` ~行 1143 | 表格新增列若 task_code 重複，Excel 匯出時行為指標可能互相覆蓋 |
| 26 | 超長文字輸入 | `wizard_ui.py` ~行 832 | `QTextEdit` 無字數上限，使用者輸入大量文字會導致 LLM prompt 超出 context window |

---

## 五、效能問題

### 🔴 高嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 27 | PDF 全文一次性載入記憶體 | `pdf_parser_v2.py` ~行 65 | `all_text += text`、`all_tables.extend(tables)` 對大型 PDF 可能 OOM |
| 28 | 表格全量清空重建 | `wizard_ui.py` ~行 1102 | `_refresh_editor_table()` 每次清空所有列再重繪，任務多時 UI 明顯卡頓 |

### 🟠 中嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 29 | 搜尋查詢無快取 | `wizard_rag.py` ~行 294 | 相同查詢每次都重新 encode，頻繁搜尋相同職業名稱浪費算力 |
| 30 | Excel 行高估算不準確 | `excel_exporter.py` ~行 145 | 以字數估算行高，不考慮欄寬，wrap_text 後實際行高與估算值不符 |

---

## 六、UI 狀態管理問題

### 🔴 高嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 31 | 返回搜尋頁後已編輯的 rows 被覆蓋 | `wizard_ui.py` ~行 1087 | `_goto_editor()` 重新呼叫 `_rows_from_standard()` 覆蓋 `_competency_rows`，使用者在編輯器的修改遺失 |
| 32 | 初始化失敗後按鈕不恢復 | `wizard_ui.py` ~行 975 | `_btn_force_rebuild` 在初始化失敗後仍保持停用狀態，使用者無法重試 |

### 🟠 中嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 33 | 切換任務前未提示儲存 | `wizard_ui.py` ~行 1217 | 使用者填寫工作描述後直接切換任務，若忘記儲存不會有警告 |
| 34 | 搜尋後返回時選擇狀態遺失 | `wizard_ui.py` ~行 1029 | 重新搜尋後，使用者前次選擇的職能基準不保留 |
| 35 | 表格無輸入驗證 | `wizard_ui.py` ~行 1109 | 職能等級欄可輸入任意文字，無範圍（1–5）驗證 |

---

## 七、Excel 輸出格式問題

### 🟠 中嚴重

| # | 問題 | 檔案與位置 | 說明 |
|---|------|-----------|------|
| 36 | 凍結窗格位置偏移 | `excel_exporter.py` 行 150 | `freeze_panes = row=9`，但實際資料標題在第 8 列（5 資訊列 + 1 空列 + 1 標題列），應為 `row=8` |
| 37 | 欄寬未考慮中文字符寬度 | `excel_exporter.py` ~行 85 | 欄寬以英文字符單位設定，中文字符實際寬度為 2 倍，可能導致部分欄位顯示截斷 |
| 38 | 行為指標含特殊字符未清理 | `excel_exporter.py` ~行 124 | 若 LLM 生成的文字含 Excel 保留字符（如 `=`、`+`），可能被誤判為公式 |

---

## 優先修復順序

### 立即處理（可能讓使用者無法使用）

| 優先 | 問題 | 位置 |
|------|------|------|
| P1 | 搜尋無結果時 IndexError crash | wizard_ui.py ~行 1069 |
| P2 | 返回搜尋頁覆蓋已編輯資料 | wizard_ui.py ~行 1087 |
| P3 | 重複點擊 LLM 分析導致混亂 | wizard_ui.py ~行 1265 |
| P4 | LLM behaviors 為空時流程卡住 | wizard_ui.py ~行 1283 |
| P5 | 初始化失敗後按鈕無法恢復 | wizard_ui.py ~行 975 |

### 短期處理（影響使用體驗）

| 優先 | 問題 | 位置 |
|------|------|------|
| P6 | 職能等級輸入無驗證 | wizard_ui.py ~行 1155 |
| P7 | Task Code 重複導致 Excel 資料異常 | wizard_ui.py ~行 1143 |
| P8 | 超長文字無上限 | wizard_ui.py ~行 832 |
| P9 | Excel 凍結窗格位置偏移 | excel_exporter.py 行 150 |
| P10 | ParseThread 失敗無 UI 回饋 | wizard_ui.py ~行 274 |

### 長期優化（穩定性與效能）

- Process 資源管理改善（Queue 清理、kill 例外處理）
- FAISS 向量維度驗證
- PDF 分頁載入（大檔案 OOM 防護）
- 搜尋查詢結果快取

---

## 統計摘要

| 嚴重度 | 數量 |
|--------|------|
| 🔴 高（可能直接 crash 或資料遺失） | 19 |
| 🟠 中（影響使用體驗或有潛在風險） | 19 |
| **合計** | **38** |
