# CompetencyWizard 修復清單

> 依據「潛在問題報告.md」整理。勾選代表已修復，並填寫修復日期與說明。

---

## 第一優先：立即處理（可能讓使用者無法使用）

- [x] **P1｜搜尋無結果時 IndexError crash**
  - 位置：`wizard_ui.py` ~行 1069
  - 問題：RAG 搜尋回傳空結果時，存取 `_std_radio_group[1]` 發生 IndexError
  - 修復日期：2026-04-22
  - 修復說明：在 `__init__` 初始化 `self._std_radio_group: List[QCheckBox] = []`，確保搜尋前不會觸發 AttributeError；同時 `_on_search_done` 的 `if not results: return` 已防止無結果時存取 `[1]`。

- [x] **P2｜返回搜尋頁會覆蓋已編輯的任務資料**
  - 位置：`wizard_ui.py` ~行 1089
  - 問題：`_goto_editor()` 重新呼叫 `_rows_from_standard()` 覆蓋 `_competency_rows`，使用者在編輯器的修改全部遺失
  - 修復日期：2026-04-22
  - 修復說明：在 `__init__` 新增 `self._last_std_code: str = ""`，`_goto_editor()` 改為只有在基準代碼改變或 `_competency_rows` 為空時才重新載入，否則保留現有編輯資料。

- [x] **P3｜重複點擊「LLM 分析」舊執行緒未停止**
  - 位置：`wizard_ui.py` ~行 1266
  - 問題：`_run_llm()` 未先 stop() 舊執行緒，新舊結果交錯覆蓋
  - 修復日期：2026-04-22
  - 修復說明：`_run_llm()` 建立新執行緒前先檢查 `_llm_thread` 是否仍在執行，若是則呼叫 `stop()`、斷開所有 signal 連線，並最多等待 2 秒後再建立新執行緒。

- [x] **P4｜AI 生成空建議時流程卡住**
  - 位置：`wizard_ui.py` ~行 1283
  - 問題：LLM 回傳空行為指標時，使用者看到紅色提示但無法繼續流程
  - 修復日期：2026-04-22
  - 修復說明：經程式碼驗證，`all_done` 信號在 `LLMAnalyzeThread.run()` 的 finally 保護下一定會發出，`_btn_confirm_suggest` 在 `_on_llm_all_done` 與 `_on_llm_error` 均有 `setEnabled(True)`，實際不存在卡住問題。

- [x] **P5｜初始化失敗後「強制重建」按鈕不恢復**
  - 位置：`wizard_ui.py` ~行 975
  - 問題：`_btn_force_rebuild` 在初始化失敗後仍保持停用，使用者無法重試
  - 修復日期：2026-04-22
  - 修復說明：經程式碼驗證，`_on_init_done()` 在成功與失敗兩個分支均有 `self._btn_force_rebuild.setEnabled(True)`，實際不存在此問題。

---

## 第二優先：短期處理（影響使用體驗）

- [x] **P6｜職能等級欄輸入無驗證**
  - 位置：`wizard_ui.py` ~行 1157
  - 問題：表格職能等級欄可輸入任意文字，`int()` 轉換非數字時 crash
  - 修復日期：2026-04-27
  - 修復說明：`_extract_rows_from_table()` 已有 `.isdigit()` 防護，再加入 `max(1, min(5, ...))` 將值鉗制在合法範圍 1–5，超出範圍的數字不再靜默寫入異常值。

- [x] **P7｜Task Code 重複導致 Excel 資料異常**
  - 位置：`wizard_ui.py` ~行 1167（`_goto_detail()`）
  - 問題：新增列若 task_code 與現有列重複，Excel 匯出時行為指標互相覆蓋
  - 修復日期：2026-04-27
  - 修復說明：`_goto_detail()` 進入詳細頁前掃描重複 task_code，若有重複則顯示含重複代碼清單的警告對話框，由使用者決定是否繼續。

- [x] **P8｜輸入文字無長度上限**
  - 位置：`wizard_ui.py` ~行 833
  - 問題：工作描述 `QTextEdit` 無字數限制，過長文字超出 LLM context window
  - 修復日期：2026-04-27
  - 修復說明：工作描述上限 800 字、工作產出上限 400 字。透過 `textChanged` 信號即時截斷超出部分，並在輸入框下方顯示「目前字數 / 上限」計數器。

- [x] **P9｜Excel 凍結窗格位置偏移**
  - 位置：`excel_exporter.py` 行 150
  - 問題：`freeze_panes = row=9`，實際標題在第 8 列，凍結位置應為 `row=8`
  - 修復日期：2026-04-27
  - 修復說明：**驗證後為誤報**。openpyxl `freeze_panes = cell(row=9)` 代表第 9 列為第一個可捲動列（rows 1–8 全部凍結），正好涵蓋標題在 row 8 的情況，現有設定正確無需修改。

- [x] **P10｜PDF 解析失敗無 UI 回饋**
  - 位置：`wizard_ui.py` `_on_parse_done()`
  - 問題：`ParseThread` 解析失敗只寫 log，使用者介面無任何錯誤提示
  - 修復日期：2026-04-27
  - 修復說明：`_on_parse_done()` 當 `err > 0` 時彈出 `QMessageBox.warning`，告知失敗數量並提示查看日誌。

---

## 第三優先：Crash 風險（特定條件下觸發）

- [x] **C1｜LM Studio 回應 content 為 None 時 crash**
  - 位置：`ai_chat.py` ~行 221
  - 問題：`resp.choices[0].message.content` 可能為 None，呼叫 `.strip()` 拋出 AttributeError
  - 修復日期：2026-04-27
  - 修復說明：**驗證後為誤報**。程式碼為 `(resp.choices[0].message.content or "").strip()`，`or ""` 已安全處理 None，無 crash 風險。

- [x] **C2｜表格新增列時上一列欄位為空導致 crash**
  - 位置：`wizard_ui.py` ~行 1122
  - 問題：`_table_add_row()` 呼叫 `prev_resp_code.text()`，若上一列該格為 None 則 AttributeError
  - 修復日期：2026-04-27
  - 修復說明：**驗證後為誤報**。程式碼為 `prev_resp_code.text() if prev_resp_code else ""`，已有 None 防護。

- [x] **C3｜LLM 生成不完整 JSON 導致 process crash**
  - 位置：`ai_chat.py` ~行 423
  - 問題：`_worker_main()` 直接 `json.loads()` 提取的 JSON，LLM 輸出截斷時拋出 JSONDecodeError
  - 修復日期：2026-04-27
  - 修復說明：**驗證後為誤報**。`json.loads()` 在 `try/except Exception` 區塊內，JSONDecodeError 被捕獲後對該任務放入空清單繼續，不會造成 process crash。

- [x] **C4｜FAISS 向量維度不匹配**
  - 位置：`wizard_rag.py` `_try_load_cache()`
  - 問題：Embedding 模型升級後舊快取維度不符，search() 時 crash，缺少啟動時驗證
  - 修復日期：2026-04-27
  - 修復說明：`_try_load_cache()` 加入維度比對：`self._index.d != self._model.get_sentence_embedding_dimension()`，維度不符時回傳 False 觸發重建，防止後續 `search()` crash。

- [x] **C5｜搜尋後選「不使用基準」、任務為空時後續 crash**
  - 位置：`wizard_ui.py` ~行 1022
  - 問題：空白開始進入編輯器後若未新增任何任務，進入 LLM 頁面時 IndexError
  - 修復日期：2026-04-27
  - 修復說明：**驗證後為誤報**。`_goto_detail()` 已有空任務列表檢查，`_goto_suggest()` 只能透過 `_detail_next()` 到達，雙重防護確保 `_competency_rows` 不為空。

---

## 第四優先：多執行緒安全

- [x] **T1｜`InitThread` 無超時機制**
  - 位置：`wizard_ui.py` `_start_init()` / `_make_loading_page()`
  - 問題：模型載入無限卡住時，UI 永遠停在載入頁，無法取消或重試
  - 修復日期：2026-04-27
  - 修復說明：載入頁新增「取消初始化」按鈕；`_start_init()` 同時啟動 5 分鐘 QTimer，逾時自動呼叫 `cancel()`，並還原按鈕狀態讓使用者重試。

- [x] **T2｜子 Process kill() 例外未處理**
  - 位置：`wizard_ui.py` `LLMAnalyzeThread.run()`
  - 問題：`proc.kill()` 在 process 已死亡時仍呼叫會拋例外；Queue 資源可能未釋放
  - 修復日期：2026-04-27
  - 修復說明：`proc.kill()` 已包在 `try/except`（原本即處理）；新增 `q.cancel_join_thread()` 呼叫，防止 Queue 背景執行緒在 process 終止後仍等待 flush 而造成資源洩漏。

- [x] **T3｜`SearchThread` 無超時**
  - 位置：`wizard_ui.py` `_on_search()`
  - 問題：RAG 搜尋卡住時 UI 完全凍結，無中斷機制
  - 修復日期：2026-04-27
  - 修復說明：`_on_search()` 啟動 30 秒 QTimer。逾時時斷開搜尋執行緒的所有 signal 連線並恢復搜尋按鈕（軟逾時，不強制 terminate 以避免 TerminateThread 危險），並顯示提示訊息讓使用者重試。

---

## 第五優先：長期優化

- [ ] **O1｜TAIDE 模型路徑硬寫 Windows 使用者路徑**
  - 位置：`ai_chat.py` ~行 39
  - 問題：路徑 `C:\Users\User\...` 限定特定帳號，移植性差（雖已有 config.toml，但預設值仍寫死）
  - 修復日期：
  - 修復說明：

- [ ] **O2｜`parsed_json_v2/` 目錄不存在時無友善提示**
  - 位置：`wizard_rag.py` ~行 25
  - 問題：預設 JSON 目錄不存在時，`_build_index()` 找到 0 個檔案並拋出 RuntimeError，缺少友善錯誤訊息
  - 修復日期：
  - 修復說明：

- [ ] **O3｜PDF 全文一次性載入記憶體**
  - 位置：`pdf_parser_v2.py` ~行 65
  - 問題：`all_text += text`、`all_tables.extend(tables)` 對大型 PDF 可能 OOM
  - 修復日期：
  - 修復說明：

- [ ] **O4｜切換任務前未提示儲存**
  - 位置：`wizard_ui.py` ~行 1217
  - 問題：使用者填寫工作描述後直接切換任務，若忘記儲存無任何警告
  - 修復日期：
  - 修復說明：

- [ ] **O5｜Excel 欄寬未考慮中文字符寬度**
  - 位置：`excel_exporter.py` ~行 85
  - 問題：欄寬以英文字符單位設定，中文字符實際寬度為 2 倍，部分欄位顯示截斷
  - 修復日期：
  - 修復說明：

- [ ] **O6｜行為指標含特殊字符未清理**
  - 位置：`excel_exporter.py` ~行 124
  - 問題：LLM 生成文字若含 `=`、`+` 等 Excel 保留字符，可能被誤判為公式
  - 修復日期：
  - 修復說明：

---

## 進度統計

| 優先級 | 總數 | 已修復 | 未修復 |
|--------|------|--------|--------|
| 第一（立即） | 5 | 5 | 0 |
| 第二（短期） | 5 | 5 | 0 |
| 第三（Crash） | 5 | 5 | 0 |
| 第四（執行緒） | 3 | 3 | 0 |
| 第五（優化） | 6 | 0 | 6 |
| **合計** | **24** | **18** | **6** |

---

*最後更新：2026-04-27*
