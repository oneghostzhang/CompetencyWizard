# AI 分析模板功能實作清單

> 功能目標：讓使用者在 Page 1 選擇 5W2H / ABCD / STAR 三種分析模板，
> AI 依選擇的模板使用不同 prompt 生成行為指標。
> 勾選代表已完成，並填寫完成日期與說明。

---

## Step 1：設計三套 Prompt（`ai_chat.py`）

- [x] **1-a｜定義 PROMPT_TEMPLATES 常數字典**
  - 位置：`ai_chat.py` — 新增於 `_build_prompt_messages()` 上方
  - 完成日期：2026-04-29
  - 完成說明：以 `PROMPT_TEMPLATES: dict[str, dict[str, str]]` 集中管理三種模板的 system/user prompt。

- [x] **1-b｜撰寫 5W2H 模板 prompt**
  - 完成日期：2026-04-29
  - 完成說明：system 要求每條指標涵蓋操作動詞、工具/系統、頻率/時機、量化標準；user 引導 AI 提取 What/How/When/HowMuch 組成句子。

- [x] **1-c｜撰寫 ABCD 模板 prompt**
  - 完成日期：2026-04-29
  - 完成說明：system 要求每條指標結構為「條件(C) + 行動(B) + 達成標準(D)」；user 引導 AI 識別三要素並組合。

- [x] **1-d｜撰寫 STAR 模板 prompt**
  - 完成日期：2026-04-29
  - 完成說明：system 要求每條指標帶情境背景與可衡量結果；user 引導 AI 從描述中找 Situation + Action + Result。

---

## Step 2：修改 `_build_prompt_messages()`（`ai_chat.py`）

- [x] **2-a｜新增 `template` 與 `level` 參數**
  - 完成日期：2026-04-29
  - 完成說明：新增 `template: str = "ABCD"`、`level: int = 3`、`user_output: str = ""`，有預設值保持向後相容。

- [x] **2-b｜依 template 選用對應 prompt**
  - 完成日期：2026-04-29
  - 完成說明：`tpl = PROMPT_TEMPLATES.get(template, PROMPT_TEMPLATES["ABCD"])`；`_LEVEL_HINT` 字典依等級加入差異化描述。

- [x] **2-c｜加入 `user_output` 參數傳入 prompt**
  - 完成日期：2026-04-29
  - 完成說明：`user_output` 納入 user_prompt context，提升 LLM 對任務目的的理解。

---

## Step 3：修改 LLM 呼叫資料流（`ai_chat.py` + `wizard_ui.py`）

- [x] **3-a｜`_worker_main()` 接收並傳遞 template/level/user_output**
  - 完成日期：2026-04-29
  - 完成說明：`analyze_tasks_batch()` 新增 `template` 參數，task_args dict 新增三個欄位，worker 解包後傳給 prompt builder。

- [x] **3-b｜`wizard_ui.py` 新增全域模板變數**
  - 完成日期：2026-04-29
  - 完成說明：`self._analysis_template: str = "ABCD"` 加入 `__init__`。

- [x] **3-c｜`LLMAnalyzeThread` 接收並傳遞 template**
  - 完成日期：2026-04-29
  - 完成說明：`LLMAnalyzeThread.__init__()` 新增 `template` 參數，`run()` 傳入 `analyze_tasks_batch()`。

- [x] **3-d｜`_run_llm()` 呼叫時帶入 template 與 user_output**
  - 完成日期：2026-04-29
  - 完成說明：`LLMAnalyzeThread` 建立時傳入 `self._analysis_template`；`_competency_rows` 中的 `output` 欄位已作為 user_output 傳入 tasks。

---

## Step 4：Page 1 新增模板選擇器 UI（`wizard_ui.py`）

- [x] **4-a｜在搜尋頁新增「AI 分析模板」區塊**
  - 完成日期：2026-04-29
  - 完成說明：`_make_search_page()` 在 subtitle 下方插入 `QGroupBox("AI 分析模板")`，包含三個 `QRadioButton`，預設選 ABCD，每個選項附一行簡短說明文字。`QRadioButton` 與 `QButtonGroup` 已加入 imports。

- [x] **4-b｜RadioButton signal 連接更新 `self._analysis_template`**
  - 完成日期：2026-04-29
  - 完成說明：`toggled` 信號連接 `_on_template_changed(key)` 方法，選取時即時更新 `self._analysis_template`，不需要額外 Apply 按鈕。

---

## Step 5：Page 4 顯示目前使用的模板（`wizard_ui.py`）

- [x] **5-a｜AI 建議頁頂部加入模板標籤**
  - 完成日期：2026-04-29
  - 完成說明：`_make_suggest_page()` 在 subtitle 下方新增 `self._tpl_label`（淺藍底色標籤）；`_run_llm()` 啟動時動態更新文字為「目前模板：ABCD｜條件＋行動＋標準」等格式。

---

## Step 6：Commit

- [ ] **6-a｜commit 所有變更**
  - 說明：`feat(prompt)：新增 5W2H / ABCD / STAR 三種 AI 分析模板`
  - 完成日期：
  - 完成說明：

---

## 進度統計

| 步驟 | 子項目數 | 已完成 | 未完成 |
|------|---------|--------|--------|
| Step 1（Prompt 設計） | 4 | 4 | 0 |
| Step 2（函式修改） | 3 | 3 | 0 |
| Step 3（資料流） | 4 | 4 | 0 |
| Step 4（UI 選擇器） | 2 | 2 | 0 |
| Step 5（UI 顯示） | 1 | 1 | 0 |
| Step 6（Commit） | 1 | 0 | 1 |
| **合計** | **15** | **14** | **1** |

---

*最後更新：2026-04-29*
